---
import ImageComponent from "./Image.astro";
import type { Image } from "../types";

interface Props {
	images: Array<Image>;
	loading?: HTMLImageElement["loading"];
}

const { images, loading = "eager" } = Astro.props;
---

<>
{
    images?.map(
      (props) => {
        return (
          <ImageComponent
            {...props}
            loading={loading}
          />
        );
      }
    )
  }
<>
