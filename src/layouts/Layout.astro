---
import { config } from "../config";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <meta name="description" content={config.description} />
    <meta property="og:title" content={config.title} />
    <meta property="og:description" content={config.description} />
    <meta property="og:image" content={config.image} />
    <meta
      property="og:image:alt"
      content={[config.title, config.description].join(", ")}
    />
    <meta property="og:locale" content="en" />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta property="og:url" content={config.url} />
    <link rel="canonical" href={config.url} />
    <link
      rel="preload"
      href="/CooperHewittBook.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  :root {
    --black: #16161d;
    --white: #fefafc;
    --slime: #b8f800;
    --gap: 1rem;
  }

  @font-face {
    font-family: "Cooper Hewitt";
    font-weight: normal;
    src:
      local("Cooper Hewitt"),
      url("/CooperHewittBook.woff2") format("woff2");
    font-display: swap;
    unicode-range: U+20-5F, U+61-7A, U+7C, U+A0, U+A7, U+A9, U+2010-2011,
      U+2013-2014, U+2018-2019, U+201C-201D, U+2020-2021, U+2026, U+2030,
      U+2032-2033, U+20AC;
  }

  * {
    font-family:
      Cooper Hewitt,
      system-ui,
      sans-serif;
    letter-spacing: 0.1em;
    line-height: 1.375;
    position: relative;
    color: var(--white);
    user-select: none;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  html {
    background-color: var(--black);
    color: var(--white);
    scroll-snap-type: y mandatory;
  }

  body {
    margin: 0;
  }

  ::selection {
    background-color: var(--slime);
    color: var(--black);
  }
</style>
